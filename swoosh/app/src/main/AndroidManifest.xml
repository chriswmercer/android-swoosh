<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.chrismercer.swoosh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.FinishActivity"
            android:label="@string/title_activity_finish"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Controller.Finish"
            android:label="@string/title_activity_finish"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Controller.SkillActivity" />
        <activity android:name=".Controller.BaseActivity" />
        <activity android:name=".Controller.LeagueActivity" />
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>